// Esperar a que el HTML termine de cargarse antes de modificar la página.
document.addEventListener("DOMContentLoaded", function () {
    // Marcar el documento como cargado para permitir aplicar estilos asociados.
    document.body.classList.add("loaded");
});
